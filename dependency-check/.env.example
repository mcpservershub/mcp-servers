# Example environment file for Dependency Check MCP Server
# Copy this to .env and configure with your settings

# NVD API Key (highly recommended for faster updates)
# Get your key at: https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=your-nvd-api-key-here

# Workspace path for scanning projects (local path on host)
WORKSPACE_PATH=./workspace

# Output path for scan reports
OUTPUT_PATH=./output

# Suppression files path
SUPPRESSION_PATH=./suppressions

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Proxy settings (if behind corporate firewall)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1